import { escapeHtml as e } from "./core.js";
import { learnerProfile, memberUser, memberService, saveLearnerProfile } from "./member.js";

export const publicRoutes = ["home", "about", "faq", "support", "privacy", "terms"];
export function recommendation(profile, lessons) {
  const language = profile.focus === "cs" ? "cs" : "js";
  const module = profile.experience === "experienced" ? 2 : profile.experience === "some" ? 1 : 0;
  return lessons.find(lesson => lesson.lang === language && lesson.module === module && !lesson.locked) || lessons.find(lesson => lesson.lang === language && !lesson.locked);
}
export function publicFooter() {
  return `<footer class="public-footer"><div><a class="public-logo" href="#home">forge<span>CODE ACADEMY</span></a><p>A little practice. A lot of possibility.</p></div><nav aria-label="Footer"><a href="#about">About</a><a href="#faq">FAQs</a><a href="#support">Support</a><a href="#privacy">Privacy</a><a href="#terms">Terms</a></nav><small>© ${new Date().getFullYear()} Forge Code Academy</small></footer>`;
}
export function renderPublic(app, route, { icon, themeToggle }) {
  const service = memberService();
  app.innerHTML = `<div class="public-site"><header class="public-header"><a class="public-logo" href="#home">forge<span>CODE ACADEMY</span></a><nav aria-label="Website navigation"><a href="#paths">Curriculum</a><a href="#premium">Pricing</a><a href="#about">About</a><a href="#faq">FAQs</a></nav><div>${themeToggle()}<a class="button secondary" href="#account">${memberUser() ? "My account" : "Sign in"}</a></div></header><main id="main" tabindex="-1"></main>${publicFooter()}</div>`;
  const main = document.querySelector("#main");
  if (route === "home") {
    main.innerHTML = `<section class="landing-hero"><div><span class="landing-tag">JAVASCRIPT + C# · FROM CURIOSITY TO CAPABILITY</span><h1>Understand the code.<br><em>Build what comes next.</em></h1><p>A focused place to learn JavaScript and C#. Read deeply, write real code, and turn what you learn into something of your own.</p><div class="landing-actions"><a class="button primary" href="#onboarding">Find your starting point ${icon("arrow", 17)}</a><a class="landing-secondary" href="#paths">Explore the curriculum →</a></div><span class="landing-footnote">Start as a guest. Make an account when you’re ready.</span></div><div class="landing-code-card" aria-label="An example of learning JavaScript and C#"><div class="code-card-top"><span>● ● ●</span><span>your-next-chapter.js</span></div><pre><span class="code-lilac">const</span> developer = {
  curiosity: <span class="code-gold">true</span>,
  practice: <span class="code-gold">"a little, every day"</span>,
  nextStep: <span class="code-gold">"build something"</span>
};

<span class="code-muted">// Start small. Keep going.</span>
learn(developer);</pre><div class="code-card-bottom"><span class="language-badge js">JS</span><span class="language-badge cs">C#</span><strong>Two languages.<br>A stronger way to think.</strong></div></div></section><section class="landing-numbers" aria-label="What is included"><div><strong>40</strong><span>in-depth lessons</span></div><div><strong>40</strong><span>coding challenges</span></div><div><strong>132</strong><span>behavior checks</span></div><div><strong>6</strong><span>project briefs</span></div></section><section class="landing-section"><div class="eyebrow">MORE THAN MEMORIZING SYNTAX</div><h2>Learn something. Then make it work.</h2><div class="landing-features">${[["book", "Understand the why", "Trace worked examples, test your predictions, and learn what happens beneath the syntax."], ["code", "Practice with feedback", "Run your code, inspect failing checks, and solve problems that include real edge cases."], ["cards", "Keep what you learn", "Write your own notes, revisit concepts with spaced review, and build a consistent routine."]].map(([ico, title, body]) => `<article class="panel">${icon(ico, 28)}<h3>${title}</h3><p>${body}</p></article>`).join("")}</div></section><section class="landing-paths"><div><div class="eyebrow">ONE SPACE. TWO PERSPECTIVES.</div><h2>From your first variable<br>to confident decisions.</h2><p>Begin with the foundations, then explore async code, data, testing, APIs, and software design.</p><a href="#paths" class="landing-secondary">See every lesson →</a></div><a class="landing-track js" href="#paths/js"><span class="language-badge js">JS</span><h3>JavaScript</h3><p>Bring ideas to life in the browser.</p><span>20 lessons · 5 modules →</span></a><a class="landing-track cs" href="#paths/cs"><span class="language-badge cs">C#</span><h3>C# & .NET</h3><p>Build systems with structure and clarity.</p><span>20 lessons · 5 modules →</span></a></section><section class="landing-cta"><div><div class="eyebrow">THE NEXT STEP CAN BE SMALL</div><h2>Make a little room for your future.</h2><p>Tell us what you want to learn. We’ll help you find a starting point.</p></div><a href="#onboarding" class="button primary">Build my study plan ${icon("arrow", 17)}</a></section>`;
    return;
  }
  const headers = { about: ["THE IDEA BEHIND FORGE", "Made for the work of learning."], faq: ["A LITTLE CLARITY", "Your questions, answered."], support: ["WE’RE HERE TO HELP", "Let’s work through it."], privacy: ["YOUR DATA", "Privacy, in plain language."], terms: ["CLEAR EXPECTATIONS", "Terms of use & subscriptions."] };
  const [eyebrow, title] = headers[route];
  main.innerHTML = `<section class="public-page-heading"><div class="eyebrow">${eyebrow}</div><h1>${title}</h1></section><div class="public-prose" id="public-prose"></div>`;
  const body = document.querySelector("#public-prose");
  if (route === "about") body.innerHTML = `<p class="prose-lead">Forge is a learning space for people who want to understand programming well enough to build with it.</p><h2>Learn by thinking, then doing</h2><p>Each lesson combines a mental model, a worked example, a concept question, a coding challenge, and reflection. You complete a lesson after passing both the concept check and the challenge.</p><h2>Why JavaScript and C#?</h2><p>JavaScript helps you understand dynamic programming and the browser. C# offers another perspective through static types, .NET, and explicit contracts. Studying both makes their tradeoffs easier to see.</p><h2>Progress you can account for</h2><p>Lesson counts and challenge results come from your activity. We do not promise a job or treat a completion badge as proof of professional mastery. Project work and continued practice are part of the journey.</p><h2>A product still growing</h2><p>Forge is in early access. Your feedback helps improve explanations, exercises, and accessibility.</p><a class="button primary" href="#feedback">Share feedback ${icon("arrow", 16)}</a>`;
  if (route === "faq") body.innerHTML = `<div class="public-faq">${[
    ["Can I start with no programming experience?", "Yes. Choose “I’m starting fresh” during setup. Each language starts with values, types, and control flow before moving into larger ideas."],
    ["Do I need an account?", "You can explore and learn as a guest. An account saves progress to this Forge service, so you can use it across devices once the site is hosted. Guest progress remains on the browser where you created it."],
    ["Will I need to install anything?", service.runner === "online" ? "JavaScript runs in the browser and C# uses a connected online compiler. Hosted C# requires a signed-in, verified account. Full project briefs may still ask you to use your own development environment." : "JavaScript runs directly in the browser. This local edition currently uses the .NET SDK for C#. An online compiler integration is included, but needs to be connected by the site owner before C# can run without a local install."],
    ["What does Premium cost?", "₦3,000 monthly or ₦28,800 yearly. The yearly plan saves ₦7,200, equivalent to ₦2,400 per month, paid once per year. Both languages are included."],
    ["Will the Premium preview charge me?", "No. The preview asks for no payment details. A subscription can start only through the separate checkout flow when payments are enabled."],
    ["How do I cancel a subscription?", "Open Account and choose “Stop automatic renewal”. Confirm the change. Paid access continues to the end of the current period. If a request fails, contact support; do not assume renewal has stopped until it is confirmed."],
    ["Can I bring my guest progress into an account?", "Yes. Sign in and use “Copy guest progress into this account” in Account. It replaces that account’s progress after confirmation, so export a backup first if you want to keep both copies."],
    ["What happens if two devices edit my progress?", "Forge checks the saved revision before syncing. If another device changed it, automatic syncing pauses and Account lets you choose which copy to keep. Notes are never silently merged over one another."],
    ["Where can I report a problem?", "Use Support or Feedback. Include the lesson name, what you expected, and what happened. Never include passwords, payment details, or private code."],
  ].map(([q, a]) => `<details><summary>${q}</summary><p>${e(a)}</p></details>`).join("")}</div><a href="#support" class="button secondary">Still need help? ${icon("arrow", 16)}</a>`;
  if (route === "support") body.innerHTML = `<p class="prose-lead">A confusing concept, a coding problem, or an account question? Tell us what happened and where you got stuck.</p><div class="support-grid"><article class="panel"><h2>Product & learning help</h2><p>Report a bug, suggest an improvement, or point out a lesson that needs a clearer explanation.</p><a class="button primary" href="#feedback">Open feedback form</a></article><article class="panel"><h2>Accounts & billing</h2><p>Check your payment history, manage renewal, or recover access to your account.</p><a href="#account">Open account →</a><a href="#forgot-password">Reset password →</a>${service.supportEmail ? `<a href="mailto:${e(service.supportEmail)}">${e(service.supportEmail)}</a>` : '<p>Direct support contact details will be published before paid launch. The feedback form is available now.</p>'}</article></div><h2>Help us understand the issue</h2><ol><li>Include the page or lesson name.</li><li>Describe what you expected and what happened.</li><li>Include the exact error text when possible.</li><li>For payment queries, include the reference from Account. Never send card details or your password.</li></ol><p>We have not published a guaranteed response time during early access.</p>`;
  if (["terms", "privacy"].includes(route)) {
    const operator = service.operatorName ? `<p><strong>Operator:</strong> ${e(service.operatorName)}${service.businessAddress ? `<br>${e(service.businessAddress)}` : ""}${service.supportEmail ? `<br>Contact: <a href="mailto:${e(service.supportEmail)}">${e(service.supportEmail)}</a>` : ""}</p>` : "";
    body.innerHTML = `<p class="policy-date">Last updated: 12 September 2026</p>${!service.legalApproved ? '<div class="policy-notice">Prelaunch policy. The owner’s business details and final policy approval are required before live subscriptions can be enabled.</div>' : ""}${operator}` + (route === "privacy" ? `<h2>Information Forge uses</h2><p>Guest lesson progress, notes, code drafts, study goals, and theme preferences are stored in your browser. If you create an account, the service also stores your name, email, password hash, learning preferences, and synced progress. Session cookies keep you signed in.</p><h2>Why we use it</h2><p>We use account details to provide access, recover accounts, sync learning, and manage subscriptions. We use saved activity to display progress and recommend study sessions. We do not sell your learning data.</p><h2>Service providers</h2><p>Account emails use Resend when configured. Submitted feedback is delivered through Web3Forms. Checkout uses Paystack; card details are entered on Paystack and are not stored by Forge. If online C# is enabled, submitted source code is sent to the configured Judge0-compatible compiler. Do not submit secrets or private code to the coding lab.</p><h2>Storage and security</h2><p>Passwords are hashed with scrypt. Session and recovery tokens are stored as hashes. Account data is stored on the Forge server; its operator is responsible for host security and backups. Browser caches may remain on a shared device after sign-out, so use a private browser profile for personal learning.</p><h2>Your choices</h2><p>You can learn without an account, export progress in Settings, edit your display name and learning preferences, and stop subscription renewal in Account. Contact Support for account deletion, correction, or an export of account information beyond the learning backup. Payment records may need to be retained for accounting or legal obligations.</p><h2>Retention and contact</h2><p>Learning records remain while your account is active or until a deletion request is completed. Session cookies expire after seven days; password reset links expire after 30 minutes. Contact the operator through Support about privacy requests. Final deployment and retention details will be published before paid launch.</p>` : `<h2>Using Forge</h2><p>Forge provides programming lessons, exercises, project briefs, and study tools. Keep your account credentials private, provide an email you control, and use the service lawfully. If you cannot legally enter a subscription agreement yourself, involve a parent or guardian before purchasing.</p><h2>Learning and code</h2><p>Completion records track practice; they are not professional certifications or guarantees of employment. You retain ownership of the notes and code you write. Do not submit malicious programs, attempt to escape the coding environment, or include secrets or third-party confidential code.</p><h2>Free and Premium access</h2><p>The planned Free tier includes eight foundation lessons and challenges, two foundation projects, playgrounds, notes, and saved progress. Premium includes the full 40 lessons and challenges, six project briefs, spaced review, and study insights. During local early access, the full curriculum may remain available. Preview activation is not a subscription.</p><h2>Prices and billing</h2><p>Premium costs ₦3,000 monthly or ₦28,800 yearly. The annual amount is paid in one payment and saves 20% against twelve monthly payments. Subscriptions renew automatically at the interval accepted during checkout until cancelled. Checkout clearly identifies test mode when no real funds are being used. Live billing remains disabled until the owner completes payment configuration and policy approval.</p><h2>Cancellation and billing issues</h2><p>Stop automatic renewal in Account before the next billing date. Paid access remains until the end of the current period. Cancellation does not automatically refund earlier payments. Contact Support for duplicate charges, errors, or refund requests; these are reviewed individually and applicable statutory rights remain unaffected.</p><h2>Availability and changes</h2><p>We aim to keep lessons and saved progress available, but outages can happen. Export backups of important work. Material changes to prices or subscription terms must be communicated before applying to a renewal. Do not use Forge’s code runner to host production workloads.</p><h2>Contact and ownership</h2><p>Use Support for product, billing, privacy, or content concerns. The operator must publish its legal identity and contact details before accepting live subscriptions.</p>`);
  }
}
let setup = null, step = 0;
export function renderOnboarding(main, { lessons, icon, toast, refresh }) {
  if (!setup) setup = { name: learnerProfile().name || "Learner", experience: "new", focus: "both", goal: "projects", minutes: 30, ...learnerProfile() };
  const headings = ["Where are you starting?", "What would you like to build?", "Make room for a little progress."];
  const options = (key, values) => `<div class="onboarding-options">${values.map(([value, title, text]) => `<label class="onboarding-option"><input type="radio" name="${key}" value="${value}" ${String(setup[key]) === String(value) ? "checked" : ""} required><span><strong>${title}</strong><small>${text}</small></span>${icon("check", 18)}</label>`).join("")}</div>`;
  main.innerHTML = `<section class="onboarding panel"><div class="setup-top"><span class="eyebrow">YOUR LEARNING, YOUR WAY</span><span>Step ${step + 1} of 3</span></div><div class="setup-progress" role="progressbar" aria-label="Learning setup" aria-valuemin="0" aria-valuemax="3" aria-valuenow="${step + 1}"><span style="width:${(step + 1) / 3 * 100}%"></span></div><h1>${headings[step]}</h1><p>${["There’s no test to pass. We’ll use this to suggest a useful first lesson.", "Pick a focus. You can change your mind and learn both at any time.", "A realistic daily goal is easier to keep. Longer lessons can span several sessions."][step]}</p><form id="onboarding-form">${step === 0 ? options("experience", [["new", "I’m starting fresh", "Begin with values, types, and the building blocks."], ["some", "I know a little code", "Work on collections, functions, and practical patterns."], ["experienced", "I’m ready to go deeper", "Explore async code and more involved concepts."]]) : step === 1 ? options("focus", [["both", "Both languages", "Compare ideas across JavaScript and C#."], ["js", "JavaScript first", "Build for the browser and the web."], ["cs", "C# first", "Practice types, .NET, and structured applications."]]) + `<label class="member-field">What brings you here?<select name="goal">${[["projects", "Build my own projects"], ["career", "Develop skills for my career"], ["foundations", "Understand programming deeply"]].map(([value, text]) => `<option value="${value}" ${setup.goal === value ? "selected" : ""}>${text}</option>`).join("")}</select></label>` : `<label class="member-field">What should we call you?<input name="name" value="${e(setup.name)}" minlength="2" maxlength="80" required autocomplete="given-name"></label>${options("minutes", [[15, "15 minutes a day", "A small, steady habit."], [30, "30 minutes a day", "Time to understand and practice."], [60, "60 minutes a day", "A focused session with room to explore."], [90, "90 minutes a day", "Deeper practice with time for reflection."]])}`}<div class="setup-actions">${step ? '<button type="button" class="button secondary" id="setup-back">Back</button>' : '<a href="#overview">Skip for now</a>'}<button class="button primary" type="submit">${step === 2 ? "Create my study plan" : "Continue"} ${icon("arrow", 17)}</button></div><p id="setup-message" role="status"></p></form></section>`;
  document.querySelector("#setup-back")?.addEventListener("click", () => { step--; renderOnboarding(main, { lessons, icon, toast, refresh }); });
  document.querySelector("#onboarding-form").addEventListener("submit", async event => {
    event.preventDefault(); Object.assign(setup, Object.fromEntries(new FormData(event.target))); setup.minutes = Number(setup.minutes);
    if (step < 2) { step++; renderOnboarding(main, { lessons, icon, toast, refresh }); return; }
    const button = event.target.querySelector('[type="submit"]'); button.disabled = true;
    try { await saveLearnerProfile({ ...setup, onboarded: true }); setup = null; step = 0; location.hash = "study-plan"; toast("Your study plan is ready. You can change it any time."); }
    catch (error) { document.querySelector("#setup-message").textContent = error.message; button.disabled = false; }
  });
}
export function resetOnboarding() { setup = null; step = 0; }
export function studyPlanCard(lessons) {
  const profile = learnerProfile();
  if (!profile.onboarded) return `<section class="welcome-setup panel"><div><span class="eyebrow">LET’S FIND YOUR STARTING POINT</span><h2>A plan that starts with you.</h2><p>Three short questions. A practical direction for your learning.</p></div><a class="button secondary" href="#onboarding">Set up my learning →</a></section>`;
  const lesson = recommendation(profile, lessons);
  return `<section class="welcome-setup panel"><div><span class="eyebrow">YOUR STUDY PLAN · ${profile.minutes} MIN / DAY</span><h2>${e(profile.name)}, make today count.</h2><p>${profile.focus === "both" ? "JavaScript + C#" : profile.focus === "cs" ? "C#" : "JavaScript"} · ${profile.goal === "career" ? "Build career skills" : profile.goal === "foundations" ? "Strengthen your understanding" : "Build your own projects"}</p></div><a class="button secondary" href="#study-plan">View my plan →</a></section>`;
}
export function renderStudyPlan(main, { lessons, icon }) {
  const profile = learnerProfile(), lesson = recommendation(profile, lessons);
  if (!profile.onboarded) { location.hash = "onboarding"; return; }
  main.innerHTML = `<section class="study-plan-hero panel"><div class="eyebrow">YOUR NEXT CHAPTER</div><h1>A little direction.<br>A lot to discover.</h1><p>${e(profile.name)}, aim for ${profile.minutes} minutes a day. Your ${profile.focus === "both" ? "JavaScript and C#" : profile.focus === "js" ? "JavaScript" : "C#"} path starts here.</p><a class="button primary" href="#lesson/${lesson.id}">Start with ${e(lesson.title)} ${icon("arrow", 16)}</a><a href="#onboarding" class="account-link">Adjust my preferences</a></section><div class="study-plan-steps">${[["01", "Build a clear mental model", "Read the example slowly. Predict what will happen before running it."], ["02", "Make it work yourself", "Write your own solution, then use the checks to investigate mistakes."], ["03", "Explain it and come back", "Save a short note. Complete the lesson and revisit its review card."], ["04", "Put the pieces together", "Choose a project brief after the foundations and build something you can explain."]].map(([n, title, body]) => `<article class="panel"><span>${n}</span><h2>${title}</h2><p>${body}</p></article>`).join("")}</div><p class="plan-guidance">This is a recommendation based on your preferences, not a placement test. You can visit any available lesson. Starting later in a path does not mark earlier lessons complete.</p><div class="setup-actions"><a href="#paths">Browse all lessons →</a>${memberUser() ? '<a href="#account">Manage my account →</a>' : '<a href="#signup">Create an account to sync my progress →</a>'}</div>`;
}
