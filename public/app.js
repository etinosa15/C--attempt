import {lessons,tracks,modules,bridges,projects} from './curriculum.js';
import {freshState,sanitizeState,dayKey,streak,scheduleReview,escapeHtml as e,highlight,STORAGE_KEY} from './core.js';

const $=s=>document.querySelector(s);
const icons={grid:'M3 3h7v7H3z M14 3h7v7h-7z M3 14h7v7H3z M14 14h7v7h-7z',path:'M4 5h6M4 12h10M4 19h16M17 3v4M14 5h6',code:'m8 7-5 5 5 5m8-10 5 5-5 5m-3-14-2 18',cards:'M7 3h13v15H7z M4 6H2v15h13v-2',folder:'M3 6h7l2 3h9v11H3z M3 6V4h7l2 2',note:'M5 3h14v18H5z M9 7h6M9 11h6M9 15h4',arrow:'M4 12h16m-6-6 6 6-6 6',chevron:'m9 5 7 7-7 7',search:'M10 3a7 7 0 1 0 0 14 7 7 0 0 0 0-14m5 12 6 6',check:'m5 12 4 4L19 6',clock:'M12 3a9 9 0 1 0 0 18 9 9 0 0 0 0-18m0 4v5l3 2',flame:'M12 3c2 6 7 6 7 12a7 7 0 0 1-14 0c0-3 2-6 4-7-1 4 1 4 1 4s3-4 2-9z',bolt:'m13 2-9 12h7l-1 8L21 9h-8z',book:'M12 5C8 2 3 3 3 3v16s5-1 9 2c4-3 9-2 9-2V3s-5-1-9 2zm0 0v16',settings:'M12 8a4 4 0 1 0 0 8 4 4 0 0 0 0-8m0-6v3m0 14v3M2 12h3m14 0h3M5 5l2 2m10 10 2 2M5 19l2-2M17 7l2-2',play:'m8 4 12 8-12 8z',close:'m6 6 12 12M6 18 18 6',download:'M12 3v12m-5-5 5 5 5-5M4 16v5h16v-5',menu:'M4 6h16M4 12h16M4 18h16',star:'m12 3 3 6 6 1-4 5 1 6-6-3-6 3 1-6-4-5 6-1z',terminal:'m5 7 5 5-5 5m8 0h6',external:'M14 3h7v7m0-7-11 11M10 3H3v18h18v-7',coffee:'M4 5h12v10a5 5 0 0 1-5 5H9a5 5 0 0 1-5-5zm12 1h2a3 3 0 0 1 0 6h-2',help:'M9 8a3 3 0 1 1 5 2c-2 1-2 2-2 4m0 3v1'};
const icon=(name,size=20)=>`<svg width="${size}" height="${size}" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.7" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><path d="${icons[name]||icons.code}"/></svg>`;
let state=freshState(),storageOK=true;
try {const raw=localStorage.getItem(STORAGE_KEY);if(raw)state=sanitizeState(JSON.parse(raw));}catch{storageOK=false;}
let status={csharp:false},route='',selectedReview=null,reviewRevealed=false,reviewAll=false,reviewedSession=0,focusRunning=false,focusInterval=null,focusRemaining=25*60,runBusy=false,currentWorker=null,output=null,outputContext=null,playLang='js';
let searchQuery='',pathFilter='all',projectFilter='all';
function save(){try{localStorage.setItem(STORAGE_KEY,JSON.stringify(state));storageOK=true;}catch{storageOK=false;toast('Progress could not be saved. Export a backup in Settings.');}}
function recordActivity(){const k=dayKey();state.activity[k]=(Number(state.activity[k])||0)+1;save();}
function toast(message){$('#toast').textContent=message;$('#toast').classList.add('visible');clearTimeout(toast.timer);toast.timer=setTimeout(()=>$('#toast').classList.remove('visible'),4000);}
const progress=lang=>{const list=lang?tracks[lang].lessons:lessons;return Math.round(list.filter(l=>state.completed.includes(l.id)).length/list.length*100);};
const nextLesson=lang=>tracks[lang].lessons.find(l=>!state.completed.includes(l.id))||tracks[lang].lessons[0];
const resumeLesson=()=>lessons.find(l=>l.id===state.lastLesson&&!state.completed.includes(l.id))||lessons.find(l=>!state.completed.includes(l.id))||lessons[0];
const dueCards=()=>lessons.filter(l=>(reviewAll||state.completed.includes(l.id))&&(!state.reviews[l.id]||Number(state.reviews[l.id].due)<=Date.now()));
const codeBlock=(code,label='Example')=>`<div class="code-block"><div class="code-label"><span>${icon('code',14)} ${e(label)}</span><span>READ · TRACE · UNDERSTAND</span></div><pre><code>${highlight(code)}</code></pre></div>`;
const badge=lang=>`<span class="language-badge ${lang}">${tracks[lang].short}</span>`;
const sectionHead=(eyebrow,title,desc='')=>`<div class="page-heading"><div class="eyebrow">${eyebrow}</div><h1>${title}</h1>${desc?`<p>${desc}</p>`:''}</div>`;
const linkButton=(url,text,cls='primary')=>`<a class="button ${cls}" href="${url}">${text}${icon('arrow',17)}</a>`;
const xp=()=>state.completed.length*100+state.solved.length*40+Object.values(state.quizzes).filter(Boolean).length*10;

function shell(){
 const parts=location.hash.slice(1).split('/');route=parts[0]||'overview';
 const titles={overview:'Overview',paths:'Learning paths',lesson:'Learning paths',playground:'Playground',review:'Review deck',projects:'Projects',project:'Projects',notebook:'Notebook',settings:'Settings'};
 const nav=[['overview','grid','Overview'],['paths','path','Learning paths'],['playground','code','Playground'],['review','cards','Review deck'],['projects','folder','Projects'],['notebook','note','Notebook']];
 $('#app').innerHTML=`<aside class="sidebar"><a href="#overview" class="brand"><span class="brand-mark">f<span>↗</span></span><span>forge<span class="brand-sub">CODE ACADEMY</span></span></a><div class="workspace-label">YOUR LEARNING SPACE</div><nav aria-label="Main navigation">${nav.map(([id,ico,label])=>`<a href="#${id}" class="nav-item ${(route===id || route==='lesson'&&id==='paths'||route==='project'&&id==='projects')?'active':''}">${icon(ico)}<span>${label}</span>${id==='review'&&dueCards().length?`<span class="nav-count">${dueCards().length}</span>`:''}${id==='playground'?'<span class="nav-dot"></span>':''}</a>`).join('')}</nav><div class="sidebar-bottom"><div class="sidebar-tip"><span class="little-spark">✳</span><strong>Small steps. Real skills.</strong><p>A little focused practice today<br>goes a long way tomorrow.</p><a href="#settings">Your daily goal <span>${state.goal} min ${icon('chevron',12)}</span></a></div><a class="nav-item settings-link ${route==='settings'?'active':''}" href="#settings">${icon('settings')}<span>Settings & backups</span></a><div class="profile"><div class="avatar">Y</div><div><strong>Your personal academy</strong><small><i class="status-dot"></i> ${storageOK?'Progress saved on this device':'Storage unavailable'}</small></div></div></div></aside><div class="main-shell"><header class="topbar"><button class="icon-button mobile-menu" aria-label="Toggle navigation" data-action="menu">${icon('menu')}</button><div class="breadcrumbs">Your workspace <span>/</span> <strong>${titles[route]||'Overview'}</strong></div><div class="topbar-right"><button class="search-trigger" data-action="search">${icon('search',17)}<span>Find a lesson</span><kbd>Ctrl K</kbd></button><div class="streak">${icon('flame',18)}<strong>${streak(state.activity)}</strong><span>day streak</span></div><div class="top-avatar">Y</div></div></header><main id="main" tabindex="-1"></main><footer class="page-footer"><span>Made for the way you learn. Built for what comes next.</span><span>JAVASCRIPT <i>×</i> C#</span></footer></div>`;
 if(route==='paths')renderPaths(parts[1]);else if(route==='lesson')renderLesson(parts[1]);else if(route==='playground')renderPlayground();else if(route==='review')renderReview();else if(route==='projects')renderProjects();else if(route==='project')renderProject(parts[1]);else if(route==='notebook')renderNotebook();else if(route==='settings')renderSettings();else renderOverview();
 window.scrollTo(0,0);
}

function heroArt(){return `<div class="hero-art" aria-hidden="true"><div class="orbit orbit-one"></div><div class="orbit orbit-two"></div><div class="orbit-dot one"></div><div class="orbit-dot two"></div><div class="float-code tiny top">const future = <em>"yours";</em></div><div class="art-tile js-tile">JS<span>create.</span></div><div class="art-tile cs-tile">C#<span>engineer.</span></div><div class="art-link">${icon('code',26)}</div><div class="float-code bottom"><span class="terminal-dot"></span> Hello, possibility<span class="cursor">▏</span></div><span class="art-spark spark-one">✳</span><span class="art-spark spark-two">+</span></div>`;}
function renderOverview(){const resume=resumeLesson();const due=lessons.filter(l=>state.completed.includes(l.id)&&(!state.reviews[l.id]||state.reviews[l.id].due<=Date.now())).length;const today=new Date();const daily=lessons[(Math.floor(Date.UTC(today.getFullYear(),today.getMonth(),today.getDate())/86400000))%lessons.length];
 $('#main').innerHTML=`<div class="overview-heading"><div><div class="eyebrow">A LITTLE PRACTICE. A LOT OF POSSIBILITY.</div><h1>Your next chapter starts here<span class="lavender-dot">.</span></h1><p>Two powerful languages. One space to make them yours.</p></div><div class="date-label">${today.toLocaleDateString('en-GB',{weekday:'short',day:'numeric',month:'long'})}</div></div><section class="hero"><div class="hero-copy"><div class="hero-kicker"><span></span> LEARN IT. WRITE IT. MAKE IT STICK.</div><h2>Don’t just learn code.<br>Learn to <em>think in it.</em></h2><p>Go from “I get it” to “I built it” with deep lessons,<br class="desktop-break"> real coding challenges, and practice that stays with you.</p><div class="hero-actions">${linkButton('#lesson/'+resume.id,state.lastLesson?'Continue learning':'Start your journey')}<span>At your pace. On your terms.</span></div></div>${heroArt()}</section><div class="stats-row"><div class="stat">${icon('book')}<div><strong>${state.completed.length}<span> / 40</span></strong><small>Lessons completed</small></div></div><div class="stat">${icon('code')}<div><strong>${state.solved.length}</strong><small>Challenges solved</small></div></div><div class="stat">${icon('bolt')}<div><strong>${xp().toLocaleString()}<span> XP</span></strong><small>Knowledge earned</small></div></div><div class="stat">${icon('clock')}<div><strong>${Math.floor(state.focusSeconds/60)}<span> min</span></strong><small>Focused practice</small></div></div></div><div class="dashboard-columns"><div class="dashboard-primary"><div class="section-title"><h2>Your learning paths <span>02</span></h2><a href="#paths">Explore curriculum ${icon('arrow',16)}</a></div><div class="course-grid">${['js','cs'].map(lang=>courseCard(lang)).join('')}</div><div class="section-title lower"><h2>A little structure. A lot of progress.</h2></div><div class="method-row">${[['01','book','Understand','Go beneath the syntax.'],['02','code','Put it to work','Write it. Run it. Refine it.'],['03','cards','Make it stick','Recall today. Retain tomorrow.']].map(([n,i,h,p])=>`<div class="method-card"><span class="method-number">${n}</span>${icon(i,21)}<h3>${h}</h3><p>${p}</p></div>`).join('')}</div><div class="bridge-banner"><span class="bridge-symbol">JS <i>⇄</i> C#</span><div><h3>Two languages. Stronger understanding.</h3><p>Compare the same ideas and see what really makes each language tick.</p></div><a href="#paths/compare" class="icon-button" aria-label="Compare JavaScript and C#">${icon('arrow')}</a></div></div><aside class="dashboard-aside"><div class="daily-card"><div class="daily-label">${icon('bolt',17)} THE DAILY CHALLENGE <span>↗</span></div><span class="mini-tag">${tracks[daily.lang].name} · ${modules[daily.module]}</span><h3>${daily.title}</h3><p>A fresh problem. A sharper mind.<br>Put your knowledge into practice.</p><a href="#lesson/${daily.id}" class="daily-button">Take the challenge ${icon('arrow',17)}</a><div class="daily-bottom"><span>${icon('clock',13)} 10–15 min practice</span><span>+40 XP</span></div></div><div class="review-card"><div class="review-card-top"><span class="review-icon">${icon('cards',22)}</span><span class="pill">ACTIVE RECALL</span></div><h3>A second look goes a long way.</h3><p>${due?`${due} learned concept${due===1?' is':'s are'} ready for review.`:'Complete a lesson to start your personal review deck.'}</p><a href="#review">${due?'Review your cards':'Explore the review deck'} ${icon('arrow',16)}</a></div><div class="daily-goal"><div><strong>Your daily focus</strong><span>${state.goal+' min goal'}</span></div><p>Protect a little time for deep work.</p><a href="#playground">Open the focus playground ${icon('arrow',15)}</a></div></aside></div>`;
}
function courseCard(lang){const tr=tracks[lang],p=progress(lang),n=nextLesson(lang);return `<article class="course-card ${lang}"><div class="course-top">${badge(lang)}<span class="course-level">BEGINNER → ADVANCED</span></div><h3>${tr.name}</h3><p>${tr.description}</p><div class="course-meta"><span>${icon('book',15)} 20 lessons</span><span>${icon('code',15)} 20 challenges</span></div><div class="course-progress-label"><span>Your progress</span><strong>${p}%</strong></div><div class="progress-track"><div style="width:${p}%"></div></div><a href="#lesson/${n.id}" class="course-link"><span>${p?'Continue path':'Start learning'}<small>${e(n.title)}</small></span>${icon('arrow',20)}</a></article>`;}
function renderPaths(filter){if(['js','cs','compare','all'].includes(filter))pathFilter=filter;
 $('#main').innerHTML=sectionHead('YOUR ROADMAP','Learn the why. Master the how.','Forty focused lessons. Every one turns a concept into something you can do.')+`<div class="tabs" role="group" aria-label="Choose curriculum"><button data-path="all" class="${pathFilter==='all'?'selected':''}">All paths</button><button data-path="js" class="${pathFilter==='js'?'selected':''}">JavaScript <span>20</span></button><button data-path="cs" class="${pathFilter==='cs'?'selected':''}">C# <span>20</span></button><button data-path="compare" class="${pathFilter==='compare'?'selected':''}">Language bridge ${icon('code',15)}</button></div>`+(pathFilter==='compare'?`<div class="bridge-grid">${bridges.map(b=>`<article class="panel bridge-detail"><h2>${b.title}</h2><div class="comparison-code">${codeBlock(b.js,'JavaScript')}${codeBlock(b.cs,'C#')}</div><p>${b.note}</p></article>`).join('')}</div>`:['js','cs'].filter(l=>pathFilter==='all'||l===pathFilter).map(lang=>`<section class="path-section"><div class="path-heading">${badge(lang)}<div><h2>${tracks[lang].name}</h2><p>${tracks[lang].tag.toLowerCase()}</p></div><span>${progress(lang)}% complete</span></div><div class="module-grid">${modules.map((module,i)=>`<div class="module panel"><div class="module-title"><span>0${i+1}</span><div><h3>${module}</h3><small>${tracks[lang].lessons.filter(l=>l.module===i).reduce((s,l)=>s+l.minutes,0)} min · 4 lessons</small></div></div>${tracks[lang].lessons.filter(l=>l.module===i).map((l,j)=>`<a class="lesson-row ${state.completed.includes(l.id)?'done':''}" href="#lesson/${l.id}"><span class="lesson-order">${state.completed.includes(l.id)?icon('check',15):String(i*4+j+1).padStart(2,'0')}</span><span>${l.title}<small>${l.minutes} min · Lesson + challenge</small></span>${icon('chevron',14)}</a>`).join('')}</div>`).join('')}</div></section>`).join(''));
}
function renderLesson(id){const l=lessons.find(l=>l.id===id);if(!l){$('#main').innerHTML='<div class="empty-state"><h1>Lesson not found</h1><a href="#paths">Browse learning paths</a></div>';return;}state.lastLesson=id;save();const list=tracks[l.lang].lessons,index=list.indexOf(l),done=state.completed.includes(id);const next=list[index+1];
 $('#main').innerHTML=`<a href="#paths/${l.lang}" class="back-link">← ${tracks[l.lang].name} learning path</a><div class="lesson-heading"><div class="eyebrow">${modules[l.module].toUpperCase()} <span>·</span> LESSON ${String(index+1).padStart(2,'0')} OF 20</div><h1>${l.title}</h1><p>${l.lead}</p><div class="lesson-meta">${badge(l.lang)}<span>${icon('clock',15)} ${l.minutes} min</span><span>${icon('bolt',15)} Up to 150 XP</span>${done?'<span class="success-text">✓ Completed</span>':''}</div></div><div class="lesson-layout"><article class="lesson-content"><section id="understand"><div class="lesson-section-heading"><span>01</span><h2>Build your mental model</h2></div>${l.sections.map(([h,p])=>`<section class="prose-section"><h3>${h}</h3><p>${e(p)}</p></section>`).join('')}${codeBlock(l.example,tracks[l.lang].name+' · worked example')}<p class="example-explanation">${e(l.explanation)}</p><div class="callout"><span>${icon('bolt',19)}</span><div><strong>Watch for this</strong><p>${e(l.trap)}</p></div></div></section><section id="predict" class="lesson-section"><div class="lesson-section-heading"><span>02</span><h2>Pause. Predict. Then check.</h2></div><div class="quiz panel"><div class="eyebrow">CHECK YOUR UNDERSTANDING</div><h3>${e(l.quiz.question)}</h3><form id="quiz-form" data-lesson="${id}"><fieldset><legend class="sr-only">Choose an answer</legend>${l.quiz.choices.map((choice,i)=>`<label class="quiz-choice"><input type="radio" name="answer" value="${i}" required><span class="choice-letter">${String.fromCharCode(65+i)}</span><span>${e(choice)}</span></label>`).join('')}</fieldset><button type="submit" class="button secondary">Check answer ${icon('arrow',16)}</button></form><div id="quiz-feedback" role="status">${state.quizzes[id]?`<div class="feedback success">${icon('check')}<p><strong>Concept checked.</strong> ${e(l.quiz.why)}</p></div>`:''}</div></div></section><section id="practice" class="lesson-section"><div class="lesson-section-heading"><span>03</span><h2>Make the code yours</h2></div><p class="challenge-prompt">${e(l.challenge.prompt)}</p>${editor(l.lang,id,l.challenge.starter,true)}<div class="hint-actions"><details><summary>A nudge in the right direction</summary><p>${e(l.sections[l.sections.length-1][1])}</p><p>Start with the normal case, then trace the empty input or boundary cases shown in the tests.</p></details><details><summary>Study the solution</summary><p>Try it yourself first. After reading, close this and rebuild the solution from memory.</p>${codeBlock(l.challenge.solution,'One possible solution')}</details></div></section><section id="reflect" class="lesson-section"><div class="lesson-section-heading"><span>04</span><h2>Explain it in your own words</h2></div><p>${e(l.recall.question)}</p><label class="sr-only" for="lesson-notes">Your notes for this lesson</label><textarea id="lesson-notes" class="notes-input" data-note="${id}" placeholder="What clicked? What would you explain to a friend? Write it here…">${e(state.notes[id]||'')}</textarea><div class="notes-caption">${icon('note',14)} Saved automatically to your notebook.</div><div class="completion-panel"><div><h3>${done?'One more concept, made yours.':'Ready to make it stick?'}</h3><p id="completion-hint">${completionHint(l)}</p></div><button class="button primary" data-complete="${id}" ${!canComplete(l)||done?'disabled':''}>${done?'Lesson complete':'Complete lesson'} ${icon('check',17)}</button></div>${done&&next?linkButton('#lesson/'+next.id,'Next: '+next.title):''}</section></article><aside class="lesson-aside"><div class="lesson-toc panel"><div class="eyebrow">THIS SESSION</div><button data-scroll="understand"><span>01</span> Understand the concept</button><button data-scroll="predict"><span>02</span> Check your intuition <i id="quiz-status">${state.quizzes[id]?'✓':''}</i></button><button data-scroll="practice"><span>03</span> Write real code <i id="code-status">${state.solved.includes(id)?'✓':''}</i></button><button data-scroll="reflect"><span>04</span> Reflect & remember</button><div class="toc-bottom">Learning happens when<br>you do the thinking.</div></div><div class="source-card">${icon('book',22)}<h3>Go to the source</h3><p>Explore the language reference when you want to go deeper.</p><a href="${l.docs}" target="_blank" rel="noopener noreferrer">${l.lang==='js'?'MDN Web Docs':'Microsoft Learn'} ${icon('external',14)}</a></div><div class="lesson-next"><small>${next?'UP NEXT':'PATH FINALE'}</small><strong>${next?next.title:'Build your capstone project'}</strong><a href="${next?'#lesson/'+next.id:'#projects'}">${next?'Preview lesson':'Explore projects'} ${icon('arrow',14)}</a></div></aside></div>`;
 bindEditor();
}
function canComplete(l){return !!state.quizzes[l.id]&&state.solved.includes(l.id);}
function completionHint(l){return state.completed.includes(l.id)?'Your review card is ready. Revisit it to strengthen your recall.':canComplete(l)?'You checked the concept and passed every code test. Add this to your review deck.':'Check the concept question and pass the coding challenge to complete this lesson.';}
function refreshCompletion(l){const b=$(`[data-complete="${l.id}"]`);if(b)b.disabled=!canComplete(l)||state.completed.includes(l.id);if($('#completion-hint'))$('#completion-hint').textContent=completionHint(l);if($('#quiz-status'))$('#quiz-status').textContent=state.quizzes[l.id]?'✓':'';if($('#code-status'))$('#code-status').textContent=state.solved.includes(l.id)?'✓':'';}
function editor(lang,key,starter,challenge=false){const draft=state.drafts[key]??starter;const same=outputContext===key&&output;
 return `<div class="editor-shell" data-editor-key="${key}" data-lang="${lang}" data-challenge="${challenge}"><div class="editor-toolbar"><span>${icon('code',16)} ${lang==='js'?'practice.js':'Program.cs'} <i class="editor-status">${lang==='js'?'JavaScript':'.NET '+(status.sdk||'')}</i></span><div><button class="editor-reset" data-action="reset-code" title="Restore starter code">Reset</button><kbd>Ctrl ↵</kbd></div></div><div class="editor-area"><div class="line-numbers" aria-hidden="true">${draft.split('\n').map((_,i)=>i+1).join('\n')}</div><label class="sr-only" for="code-editor">${tracks[lang].name} code editor</label><textarea id="code-editor" spellcheck="false" autocapitalize="off" autocomplete="off" autocorrect="off" wrap="off">${e(draft)}</textarea></div><div class="editor-bottom"><span><i class="status-dot"></i> ${lang==='js'?'Runs in an isolated worker':'Runs locally on your computer · use code you trust'}</span><div><button class="button editor-run" data-action="run-code" ${runBusy?'disabled':''}>${icon('play',14)} Run code</button>${challenge?`<button class="button primary" data-action="test-code" ${runBusy?'disabled':''}>${icon('check',16)} Check solution</button>`:''}</div></div><div class="output-panel"><div class="output-heading"><span>${icon('terminal',15)} OUTPUT ${challenge?'<i>& TEST RESULTS</i>':''}</span><button data-action="clear-output" aria-label="Clear output">Clear</button></div><div id="code-output" role="status" aria-live="polite">${same?outputMarkup(output):'<div class="output-empty">Your next discovery starts with a run.<span>Write some code, then see what happens.</span></div>'}</div></div></div>`;
}
function bindEditor(){const ed=$('#code-editor');if(!ed)return;const holder=$('.editor-shell');ed.addEventListener('input',()=>{state.drafts[holder.dataset.editorKey]=ed.value;save();$('.line-numbers').textContent=ed.value.split('\n').map((_,i)=>i+1).join('\n');});ed.addEventListener('scroll',()=>{$('.line-numbers').scrollTop=ed.scrollTop;});ed.addEventListener('keydown',ev=>{if(ev.key==='Tab'){ev.preventDefault();const start=ed.selectionStart,end=ed.selectionEnd;ed.setRangeText('  ',start,end,'end');ed.dispatchEvent(new Event('input'));}if(ev.key==='Enter'&&(ev.ctrlKey||ev.metaKey)){ev.preventDefault();runCode(holder.dataset.challenge==='true');}});}
function outputMarkup(data){return `${data.error?`<div class="run-error"><strong>Something to investigate</strong><pre>${e(data.error)}</pre></div>`:''}${data.logs?.length?`<pre class="console-lines">${e(data.logs.join('\n'))}</pre>`:''}${data.results?.length?`<div class="test-summary ${data.results.every(t=>t.passed)?'passed':'failed'}">${icon(data.results.every(t=>t.passed)?'check':'code',17)} ${data.results.filter(t=>t.passed).length} / ${data.results.length} checks passed</div>${data.results.map(t=>`<div class="test-result ${t.passed?'pass':'fail'}"><span>${t.passed?'✓':'×'}</span><div><strong>${e(t.label)}</strong>${!t.passed?`<small>Expected: ${e(JSON.stringify(t.expected))}<br>Received: ${e(t.error||JSON.stringify(t.actual)||'undefined')}</small>`:''}</div></div>`).join('')}`:!data.error&&!data.logs?.length?'<div class="output-empty">Finished successfully.<span>Use console.log or Console.WriteLine to display a value.</span></div>':''}${data.warnings?`<details class="compiler-warnings"><summary>Compiler notes</summary><pre>${e(data.warnings)}</pre></details>`:''}`;}
async function runCode(check=false){if(runBusy)return;const holder=$('.editor-shell');if(!holder)return;const code=$('#code-editor').value,lang=holder.dataset.lang,key=holder.dataset.editorKey;const lesson=lessons.find(l=>l.id===key);if(check&&!lesson)return;runBusy=true;holder.querySelectorAll('.editor-bottom button').forEach(b=>b.disabled=true);$('#code-output').innerHTML=`<div class="running"><span class="spinner"></span>${lang==='cs'?'Compiling and running C#…':'Running your JavaScript…'}</div>`;
 let result;
 try{
  if(lang==='js')result=await new Promise(resolve=>{const worker=new Worker('/runner-worker.js');currentWorker=worker;const timer=setTimeout(()=>{worker.terminate();resolve({error:'Execution stopped after 4 seconds. Check for an infinite loop or unresolved promise.',logs:[]});},4000);const finish=data=>{clearTimeout(timer);worker.terminate();currentWorker=null;resolve(data);};worker.onmessage=ev=>finish(ev.data);worker.onerror=ev=>finish({error:ev.message||'The JavaScript worker could not start.'});worker.postMessage({code,tests:check?lesson.challenge.tests:[]});});
  else{if(!status.token)status=await fetch('/api/status').then(r=>r.json());const r=await fetch('/api/run',{method:'POST',headers:{'Content-Type':'application/json','X-Forge-Token':status.token},body:JSON.stringify({code,lessonId:check?key:undefined})});result=await r.json();}
 }catch(err){result={error:'Could not reach the local runner. Keep the Forge server window open. '+err.message};}
 runBusy=false;output=result;outputContext=key;
 document.querySelectorAll('.editor-bottom button').forEach(b=>b.disabled=false);
 if(check&&!result.error&&result.results?.length===lesson.challenge.tests.length&&result.results.every(t=>t.passed)){if(!state.solved.includes(key)){state.solved.push(key);recordActivity();toast('Challenge solved. +40 XP — well earned.');}else save();}
 if($('.editor-shell')?.dataset.editorKey===key){$('#code-output').innerHTML=outputMarkup(result);$('.editor-shell').querySelectorAll('.editor-bottom button').forEach(b=>b.disabled=false);if(lesson)refreshCompletion(lesson);}
}
const playgroundStarters={js:'// Your space to experiment. Change something. Run it again.\nconst skills = ["curiosity", "practice", "persistence"];\n\nfunction buildSomething(ingredients) {\n  return ingredients.map(skill => skill.toUpperCase());\n}\n\nconsole.log("Hello, possibility.");\nconsole.log(buildSomething(skills));\n',cs:'// Real C#, compiled and run with your local .NET SDK.\nvar skills = new[] { "curiosity", "practice", "persistence" };\n\nConsole.WriteLine("Hello, possibility.");\nforeach (var skill in skills)\n{\n    Console.WriteLine(skill.ToUpperInvariant());\n}\n'};
function renderPlayground(){const p=new URLSearchParams(location.hash.split('?')[1]);if(['js','cs'].includes(p.get('lang')))playLang=p.get('lang');
 $('#main').innerHTML=sectionHead('MAKE ROOM FOR EXPERIMENTS','A blank canvas. A working mind.','Try an idea, break something, follow your curiosity. Your drafts stay right here.')+`<div class="playground-layout"><section><div class="playground-controls"><div class="tabs"><button data-play-lang="js" class="${playLang==='js'?'selected':''}">JavaScript</button><button data-play-lang="cs" class="${playLang==='cs'?'selected':''}">C# / .NET</button></div><span class="runtime-label">${icon('check',14)} ${playLang==='js'?'Browser runtime':status.csharp?'Real .NET compiler':'SDK not available'}</span></div>${editor(playLang,'play-'+playLang,playgroundStarters[playLang])}<div class="playground-note">${icon('help',18)}<p>${playLang==='js'?'This is a JavaScript console, without a DOM. Top-level await is supported. Await asynchronous work before the run ends. For DOM practice, use the browser lab below.':'Write a complete console program. Common System namespaces are imported for you. Put top-level statements before class and record declarations. ASP.NET projects belong in their own project folder.'}</p></div>${playLang==='js'?`<section class="dom-lab panel"><div class="section-title"><h2>A little browser lab</h2><span class="pill">DOM + EVENTS</span></div><p>Connect an actual button to the page. This preview is isolated from your learning data.</p><label for="dom-html">HTML</label><textarea class="mini-editor" id="dom-html" spellcheck="false">${e(state.drafts['dom-html']??'<h2>Make something happen.</h2>\n<button>Say hello</button>\n<output></output>')}</textarea><label for="dom-js">JavaScript</label><textarea class="mini-editor" id="dom-js" spellcheck="false">${e(state.drafts['dom-js']??'document.querySelector("button").addEventListener("click", () => {\n  document.querySelector("output").textContent = "Hello, developer!";\n});')}</textarea><button class="button primary" data-action="dom-run">${icon('play',15)} Update preview</button><iframe title="Isolated DOM practice preview" id="dom-preview" src="/dom-preview.html" sandbox="allow-scripts"></iframe><pre id="dom-output" role="status"></pre></section>`:''}</section><aside><div class="focus-card panel"><span class="focus-icon">${icon('coffee',23)}</span><div class="eyebrow">ONE THING AT A TIME</div><h3>A little focus goes far.</h3><div id="focus-time" class="focus-time">${formatTime(focusRemaining)}</div><p>25 minutes to get into the flow.<br>Then give your mind a break.</p><button class="button primary" data-action="focus">${focusRunning?'Pause session':'Start focus session'} ${icon('play',15)}</button><button class="text-button" data-action="focus-reset">Reset timer</button></div><div class="panel prompts-card"><h3>Follow a small question.</h3><p>What happens if the input is empty?</p><p>Can I explain each line out loud?</p><p>What is the simplest version that works?</p><p>How would I test this behavior?</p></div><a class="play-project-link" href="#projects">Ready for something bigger? ${icon('arrow',17)}<strong>Pick a project.</strong></a></aside></div>`;bindEditor();
 for(const id of ['dom-html','dom-js'])$('#'+id)?.addEventListener('input',ev=>{state.drafts[id]=ev.target.value;save();});
}
function formatTime(n){return String(Math.floor(n/60)).padStart(2,'0')+':'+String(n%60).padStart(2,'0');}
function toggleFocus(){focusRunning=!focusRunning;if(focusRunning){if(focusRemaining<=0)focusRemaining=1500;let last=Date.now();focusInterval=setInterval(()=>{const now=Date.now(),elapsed=Math.floor((now-last)/1000);if(!elapsed)return;last+=elapsed*1000;const used=Math.min(focusRemaining,elapsed);state.focusSeconds+=used;focusRemaining-=used;if(focusRemaining%15===0)save();if($('#focus-time'))$('#focus-time').textContent=formatTime(focusRemaining);if(focusRemaining<=0){clearInterval(focusInterval);focusRunning=false;recordActivity();toast('Focus session complete. Stand up, stretch, and take a break.');if($('[data-action="focus"]'))$('[data-action="focus"]').innerHTML='Start another session';}},1000);}else{clearInterval(focusInterval);save();}if($('[data-action="focus"]'))$('[data-action="focus"]').innerHTML=(focusRunning?'Pause session':'Start focus session')+icon('play',15);}

function renderReview(){let cards=dueCards();if(!cards.some(l=>l.id===selectedReview?.id))selectedReview=cards[0];
 $('#main').innerHTML=sectionHead('RECALL IS WHERE LEARNING STICKS','Meet your future memory.','Try to retrieve the answer before you reveal it. The effort is the learning.')+`<div class="review-toolbar"><div class="tabs"><button data-review-mode="learned" class="${!reviewAll?'selected':''}">My learned concepts</button><button data-review-mode="all" class="${reviewAll?'selected':''}">Explore all cards</button></div><span>${cards.length} due · ${reviewedSession} reviewed this session</span></div><div class="review-layout"><section>${selectedReview?`<article class="flashcard"><div class="flashcard-top">${badge(selectedReview.lang)}<span>${modules[selectedReview.module]}</span><span>RECALL CARD</span></div><div class="flashcard-body"><div class="eyebrow">WITHOUT LOOKING IT UP…</div><h2>${e(selectedReview.recall.question)}</h2>${reviewRevealed?`<div class="recall-answer">${e(selectedReview.recall.answer)}</div>`:'<p>Take a moment. Say the answer out loud, or write it down.</p>'}</div><div class="flashcard-bottom">${!reviewRevealed?`<button class="button primary" data-action="reveal">Reveal answer ${icon('arrow',16)}</button><span>Space to reveal</span>`:`<span>How well did you remember?</span><div class="rating-buttons"><button data-rating="again">Again<small>10 minutes</small></button><button data-rating="hard">With effort<small>Tomorrow</small></button><button data-rating="good">Got it<small>${scheduleReview(state.reviews[selectedReview.id],'good').interval} day${scheduleReview(state.reviews[selectedReview.id],'good').interval===1?'':'s'}</small></button></div>`}</div></article><a class="review-source" href="#lesson/${selectedReview.id}">${icon('book',16)} Revisit: ${selectedReview.title} ${icon('arrow',15)}</a>`:`<div class="empty-state panel"><div class="empty-icon">${icon('cards',36)}</div><h2>${state.completed.length||reviewAll?'You’re all caught up.':'Build knowledge. Then keep it.'}</h2><p>${state.completed.length||reviewAll?'Your cards will return when they’re due. Good learning includes a little space.':'Complete your first lesson to add a concept to your personal review deck. Or explore all cards for a preview.'}</p>${linkButton('#lesson/'+resumeLesson().id,'Continue learning')}</div>`}</section><aside class="review-explainer panel"><span class="little-spark">✳</span><h3>A bit of forgetting is useful.</h3><p>Recalling an answer strengthens it more than simply reading it again.</p><ol><li>Attempt an answer before revealing.</li><li>Rate your actual recall, honestly.</li><li>Return when the card is due.</li></ol><div class="callout-small">Again returns in 10 minutes. With effort returns tomorrow. Got it grows the interval up to 60 days.</div></aside></div>`;
}
function renderProjects(){const filtered=projects.filter(p=>projectFilter==='all'||p.lang===projectFilter);$('#main').innerHTML=sectionHead('FROM UNDERSTANDING TO OWNERSHIP','Make something that matters.','Six project briefs. Real requirements. A reason to bring everything together.')+`<div class="tabs"><button data-project-filter="all" class="${projectFilter==='all'?'selected':''}">All projects</button><button data-project-filter="js" class="${projectFilter==='js'?'selected':''}">JavaScript</button><button data-project-filter="cs" class="${projectFilter==='cs'?'selected':''}">C# & full stack</button></div><div class="projects-grid">${filtered.map((p,i)=>{const checks=state.projectChecks[p.id]||[];return `<a href="#project/${p.id}" class="project-card panel"><div class="project-art project-art-${projects.indexOf(p)%3}"><span class="project-art-code">${['{ habits }','₦ 12,500','fetch( )','/api/books','recall( )','JS ⇄ C#'][projects.indexOf(p)]}</span><span class="project-level">${p.level}</span></div><div class="project-body"><div class="project-meta">${badge(p.lang)}<span>${p.time}</span></div><h2>${p.title}</h2><p>${p.summary}</p><div class="skill-tags">${p.skills.map(s=>`<span>${s}</span>`).join('')}</div><div class="project-link"><span>${checks.length?checks.length+' / '+p.steps.length+' milestones':'Open project brief'}</span>${icon('arrow',18)}</div></div></a>`;}).join('')}</div>`;}
function renderProject(id){const p=projects.find(x=>x.id===id);if(!p){renderProjects();return;}const checks=state.projectChecks[id]||[];$('#main').innerHTML=`<a class="back-link" href="#projects">← All projects</a>`+sectionHead(p.level.toUpperCase()+' PROJECT · '+p.time,p.title,p.summary)+`<div class="project-detail-layout"><article class="panel project-brief"><h2>The brief</h2><p>${p.brief}</p><div class="skill-tags">${p.skills.map(s=>`<span>${s}</span>`).join('')}</div><h2>Build it one milestone at a time.</h2><p class="muted">Mark a milestone when you have verified the behavior in your own project.</p><div class="milestones">${p.steps.map((s,i)=>`<label><input type="checkbox" data-project="${id}" data-step="${i}" ${checks.includes(i)?'checked':''}><span><small>MILESTONE ${i+1}</small>${s}</span></label>`).join('')}</div><div class="callout"><span>${icon('star',19)}</span><div><strong>Stretch your skills</strong><p>${p.stretch}</p></div></div><h2>Your project notes</h2><textarea class="notes-input" data-note="project-${id}" placeholder="Decisions, questions, discoveries…">${e(state.notes['project-'+id]||'')}</textarea></article><aside><div class="panel project-launch"><div class="eyebrow">YOUR STARTING POINT</div><h3>Start small. Keep building.</h3><p>Use the playground to test a core rule, then create the full project in its own folder.</p><button class="button primary" data-project-start="${id}">Try a starting idea ${icon('arrow',16)}</button><a href="${p.resources}" target="_blank" rel="noopener noreferrer">Read the official guide ${icon('external',14)}</a></div><div class="panel project-rubric"><h3>Before you call it done</h3><p>Can someone else run it from your README?</p><p>Does it handle invalid and empty input?</p><p>Are the important rules covered by tests?</p><p>Can you explain one tradeoff you made?</p></div></aside></div>`;}
function renderNotebook(){const entries=Object.entries(state.notes).filter(([,v])=>v.trim());$('#main').innerHTML=sectionHead('YOUR WORDS. YOUR UNDERSTANDING.','Leave a trail of what you learn.','The explanation you write yourself is often the one you remember.')+`<div class="notebook-layout"><section class="panel personal-notes"><div class="section-title"><h2>Your scratchpad</h2><span class="pill">AUTOSAVED</span></div><p>Ideas, questions, and things to try next.</p><textarea data-note="scratchpad" class="notes-input scratchpad" placeholder="Today I learned…">${e(state.notes.scratchpad||'')}</textarea></section><section class="lesson-notes-list"><div class="section-title"><h2>From your lessons & projects</h2><span>${entries.filter(([id])=>id!=='scratchpad').length} notes</span></div>${entries.filter(([id])=>id!=='scratchpad').map(([id,note])=>{const l=lessons.find(x=>x.id===id),p=projects.find(x=>'project-'+x.id===id);return `<a class="saved-note panel" href="${l?'#lesson/'+id:p?'#project/'+p.id:'#notebook'}"><small>${l?tracks[l.lang].name:p?'PROJECT':'NOTE'}</small><h3>${e(l?.title||p?.title||id)}</h3><p>${e(note.slice(0,240))}${note.length>240?'…':''}</p><span>Continue your thinking ${icon('arrow',14)}</span></a>`;}).join('')||'<div class="panel empty-note">Your lesson notes will appear here.<br>A thought worth saving is a thought worth revisiting.</div>'}</section></div>`;}
function renderSettings(){$('#main').innerHTML=sectionHead('MAKE THIS SPACE YOURS','A little setup. A steady rhythm.','Your learning stays on this device. Keep a backup when you want to take it elsewhere.')+`<div class="settings-grid"><section class="panel settings-panel"><div class="settings-icon">${icon('clock',24)}</div><h2>Set your daily intention</h2><p>Choose a realistic amount of focused practice. Consistency matters more than a heroic first day.</p><div class="goal-options">${[15,30,60,90].map(g=>`<button data-goal="${g}" class="${state.goal===g?'selected':''}">${g}<small>min / day</small></button>`).join('')}</div><p class="muted">Use the 25-minute timer in the playground to track focused practice.</p></section><section class="panel settings-panel"><div class="settings-icon">${icon('download',24)}</div><h2>Your progress, portable</h2><p>Back up completed lessons, quiz results, notes, code drafts, project milestones, and review schedules.</p><div class="settings-actions"><button class="button primary" data-action="export">${icon('download',16)} Export progress</button><button class="button secondary" data-action="import">Import backup</button><input type="file" id="import-file" accept="application/json,.json" hidden></div><p class="muted">Import replaces current progress. Export first if you want to preserve it.</p></section><section class="panel settings-panel"><div class="settings-icon">${icon('terminal',24)}</div><h2>Your local learning studio</h2><div class="runtime-info"><span>JavaScript</span><strong>Ready · isolated worker</strong><span>C# compiler</span><strong>${status.csharp?'Ready · .NET SDK '+e(status.sdk):'Not available — install .NET SDK'}</strong><span>Account required</span><strong>No</strong><span>Internet required</span><strong>No, except reference links</strong><span>Progress storage</span><strong>${storageOK?'This browser · localStorage':'Unavailable · export a backup'}</strong></div><p class="muted">C# runs with your local user’s permissions. Use code you trust. This app is designed for personal use on localhost.</p></section><section class="panel settings-panel"><div class="settings-icon">${icon('path',24)}</div><h2>A study rhythm that works</h2><ol class="study-rhythm"><li><strong>5 minutes</strong> Recall a previous concept before rereading.</li><li><strong>10–20 minutes</strong> Read and trace one worked example.</li><li><strong>15–30 minutes</strong> Solve a challenge without copying.</li><li><strong>5 minutes</strong> Explain what changed in your understanding.</li></ol><p class="muted">Lesson timings are estimates. Slow down when a concept deserves another pass.</p></section></div>`;}

function openSearch(){if($('#search-dialog'))return;const d=document.createElement('dialog');d.id='search-dialog';d.innerHTML=`<div class="search-dialog-top">${icon('search')}<label class="sr-only" for="lesson-search">Search lessons</label><input id="lesson-search" placeholder="Search topics, languages, or ideas…" autocomplete="off"><button class="icon-button" data-action="close-search" aria-label="Close search">${icon('close',18)}</button></div><div id="search-results"></div><div class="search-dialog-foot">Search all 40 lessons <kbd>Esc to close</kbd></div>`;document.body.append(d);d.showModal();searchResults('');$('#lesson-search').addEventListener('input',ev=>searchResults(ev.target.value));d.addEventListener('close',()=>d.remove());d.addEventListener('click',ev=>{if(ev.target===d)d.close();});}
function searchResults(query){const terms=query.toLowerCase().trim().split(/\s+/);const found=lessons.filter(l=>terms.every(q=>(tracks[l.lang].name+' '+l.title+' '+l.lead+' '+modules[l.module]+' '+l.sections.map(s=>s.join(' ')).join(' ')).toLowerCase().includes(q))).slice(0,12);$('#search-results').innerHTML=found.length?found.map(l=>`<a class="search-result" href="#lesson/${l.id}">${badge(l.lang)}<span><strong>${l.title}</strong><small>${modules[l.module]} · ${l.minutes} min</small></span>${icon('arrow',16)}</a>`).join(''):'<div class="empty-note">No lessons match that search. Try “async”, “types”, or “testing”.</div>';}

document.addEventListener('click',async ev=>{
 const button=ev.target.closest('button,a');if(!button)return;
 if(button.classList.contains('skip-link')){ev.preventDefault();$('#main').focus();return;}
 if(button.closest('#search-results'))$('#search-dialog').close();
 const a=button.dataset.action;
 if(a==='menu')$('.sidebar').classList.toggle('mobile-open');
 if(a==='search')openSearch();if(a==='close-search')$('#search-dialog')?.close();
 if(button.dataset.path){pathFilter=button.dataset.path;renderPaths();}
 if(button.dataset.scroll)document.getElementById(button.dataset.scroll)?.scrollIntoView({behavior:'smooth',block:'start'});
 if(a==='run-code')runCode();if(a==='test-code')runCode(true);
 if(a==='clear-output'){output=null;$('#code-output').innerHTML='<div class="output-empty">Output cleared.</div>';}
 if(a==='reset-code'){const holder=$('.editor-shell'),key=holder.dataset.editorKey;const draft=$('#code-editor').value;const starter=lessons.find(l=>l.id===key)?.challenge.starter||playgroundStarters[holder.dataset.lang];if(draft!==starter&&!confirm('Restore the starter code? Your current draft for this exercise will be replaced.'))return;state.drafts[key]=starter;save();$('#code-editor').value=starter;$('#code-editor').dispatchEvent(new Event('input'));toast('Starter code restored.');}
 if(button.dataset.complete){const l=lessons.find(x=>x.id===button.dataset.complete);if(l&&canComplete(l)&&!state.completed.includes(l.id)){state.completed.push(l.id);state.reviews[l.id]={count:0,interval:0,due:Date.now()};recordActivity();toast('Lesson completed. +100 XP. Your review card is ready.');renderLesson(l.id);$('#reflect').scrollIntoView({behavior:'smooth'});}}
 if(button.dataset.playLang){playLang=button.dataset.playLang;renderPlayground();}
 if(a==='focus')toggleFocus();if(a==='focus-reset'){if(focusRunning)toggleFocus();focusRemaining=1500;if($('#focus-time'))$('#focus-time').textContent=formatTime(focusRemaining);}
 if(a==='dom-run'){const frame=$('#dom-preview');frame.contentWindow.postMessage({type:'forge-dom',html:$('#dom-html').value,code:$('#dom-js').value},'*');}
 if(button.dataset.reviewMode){reviewAll=button.dataset.reviewMode==='all';selectedReview=null;reviewRevealed=false;renderReview();}
 if(a==='reveal'){reviewRevealed=true;renderReview();}
 if(button.dataset.rating&&selectedReview&&reviewRevealed){state.reviews[selectedReview.id]=scheduleReview(state.reviews[selectedReview.id],button.dataset.rating);recordActivity();reviewedSession++;selectedReview=null;reviewRevealed=false;renderReview();}
 if(button.dataset.projectFilter){projectFilter=button.dataset.projectFilter;renderProjects();}
 if(button.dataset.projectStart){const p=projects.find(x=>x.id===button.dataset.projectStart);const key='play-'+p.lang;if(state.drafts[key]&&state.drafts[key]!==p.starter&&!confirm('Replace your '+tracks[p.lang].name+' playground draft with this project starter?'))return;state.drafts[key]=p.starter;save();playLang=p.lang;location.hash='playground';}
 if(button.dataset.goal){state.goal=Number(button.dataset.goal);save();renderSettings();toast('Daily goal updated.');}
 if(a==='export'){const blob=new Blob([JSON.stringify(state,null,2)],{type:'application/json'});const url=URL.createObjectURL(blob);const link=document.createElement('a');link.href=url;link.download='forge-progress-'+dayKey()+'.json';link.click();setTimeout(()=>URL.revokeObjectURL(url),1000);toast('Progress backup exported.');}
 if(a==='import')$('#import-file').click();
});
document.addEventListener('input',ev=>{if(ev.target.dataset.note){state.notes[ev.target.dataset.note]=ev.target.value;save();}});
document.addEventListener('change',async ev=>{
 const el=ev.target;
 if(el.dataset.project){const id=el.dataset.project,step=Number(el.dataset.step);const set=new Set(state.projectChecks[id]||[]);el.checked?set.add(step):set.delete(step);state.projectChecks[id]=[...set];save();}
 if(el.id==='import-file'){const file=el.files[0];if(!file)return;try{if(file.size>5e6)throw new Error('Backup exceeds the 5 MB limit.');const imported=sanitizeState(JSON.parse(await file.text()));if(!confirm('Replace your current progress with this backup? Export your current progress first if you want to keep it.'))return;state=imported;save();shell();toast('Progress restored from your backup.');}catch(err){toast('Could not import: '+err.message);}finally{el.value='';}}
});
document.addEventListener('submit',ev=>{if(ev.target.id!=='quiz-form')return;ev.preventDefault();const l=lessons.find(x=>x.id===ev.target.dataset.lesson),answer=Number(new FormData(ev.target).get('answer'));const correct=answer===l.quiz.answer;if(correct&&!state.quizzes[l.id]){state.quizzes[l.id]=true;recordActivity();toast('Concept checked. +10 XP.');}$('#quiz-feedback').innerHTML=`<div class="feedback ${correct?'success':'retry'}">${icon(correct?'check':'help')}<p><strong>${correct?'That’s it.':'Not quite. Trace the rule again.'}</strong> ${e(l.quiz.why)}</p></div>`;refreshCompletion(l);});
document.addEventListener('keydown',ev=>{if((ev.ctrlKey||ev.metaKey)&&ev.key.toLowerCase()==='k'){ev.preventDefault();openSearch();}if(ev.code==='Space'&&route==='review'&&!reviewRevealed&&selectedReview&&!['INPUT','TEXTAREA','BUTTON'].includes(document.activeElement.tagName)&&!$('#search-dialog')){ev.preventDefault();reviewRevealed=true;renderReview();}});
window.addEventListener('message',ev=>{if(ev.source!==$('#dom-preview')?.contentWindow||ev.data?.type!=='forge-dom-output')return;$('#dom-output').textContent=ev.data.message;});
window.addEventListener('hashchange',()=>{reviewRevealed=false;shell();});
window.addEventListener('beforeunload',()=>save());
shell();
fetch('/api/status').then(r=>r.json()).then(data=>{status=data;if(route==='settings')renderSettings();}).catch(()=>{});
